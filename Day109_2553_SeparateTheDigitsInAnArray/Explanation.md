# 2553. Separate the Digits in an Array

Approach summary:

- Traverse each number in `nums`.
- Extract its digits from right to left.
- Reverse the extracted digits before appending them to the result.
- Return the collected digits as an `int[]`.
